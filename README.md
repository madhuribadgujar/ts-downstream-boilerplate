# ts-downstream-boilerplate

A Node.js + TypeScript boilerplate for calling downstream APIs using
Controller → Service → Adapter pattern with Circuit Breaker.

## Usage

npx ts-downstream-boilerplate my-app
cd my-app
npm install
npm run dev

git init
git add .
git commit -m "Initial boilerplate generator"
# ts-downstream-boilerplate
